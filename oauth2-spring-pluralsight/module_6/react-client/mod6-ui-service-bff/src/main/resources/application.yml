spring:
  application:
    name: ui-service

  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri:  http://localhost:8080/auth/realms/CryptoInc
          
server:
   port: 8185